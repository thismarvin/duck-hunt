const screenW = 400;
const screenH = 400;

function setup() {
    createCanvas(screenW, screenH);
    //prompt("SUP MARVIN");
    // what up
    // my name is austin
}

function draw() {
    background(0);
}
